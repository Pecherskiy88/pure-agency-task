export const stepSelector = (state) => state.questionnaire.step;

export const employmentSelector = (state) =>
  state.questionnaire.employmentValue;

export const proprietorshipsSelector = (state) =>
  state.questionnaire.proprietorshipsValue;

export const filingStatusSelector = (state) =>
  state.questionnaire.filingStatusData;
